

export type Atividade = {
    id: number;
    descricao: string;
    nome: string; 
    flagconsfinal: boolean;
    flagprecoatacado: boolean;
    flagambulante: boolean;
    flagempenho: boolean;
    flagativo: boolean; 
};

 